export class Artist {
    band: string;
    genre: string;
    id: any;
    votes: number;
    approved: number;

    constructor(band: string, genre: string, votes: number, approved: number) {
      this.band = name;
      this.genre = genre;
      this.votes = votes;
      this.approved = approved;

    }
  }